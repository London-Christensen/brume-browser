# Releasing Brume

> This document is filled in properly once the auto-updater is wired up (build step 4).
> It will cover: bumping the version, building signed artifacts, generating `latest.json`,
> and publishing to GitHub Releases so existing installs pick up the update automatically.

## Placeholder

Nothing to release yet — the installer pipeline is not built at the time of writing.
