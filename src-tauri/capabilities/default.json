{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions granted to the Brume browser chrome window. Tauri 2 denies everything that is not explicitly listed here, so this file grows deliberately as features are added rather than being opened up broadly up front.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
